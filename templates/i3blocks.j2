#
# This file has been auto-generated by i3 wm auto configurer
# It will be overwritten, so visit 
#

# Global properties
command={{ home }}/.config/i3/blocks/$BLOCK_NAME
separator_block_width=15
markup=none

# Volume indicator
#
# The first parameter sets the step (and units to display)
# The second parameter overrides the mixer selection
# See the script for details.
[volume]
#label=VOL
label=♪
instance=Master
#instance=PCM
interval=once
signal=10

# btc price
[btcprice.py]
interval=300
markup=pango

[newpackages]
interval=3600
markup=pango

[dc_show]
interval=10
markup=pango

# Memory usage
#
# The type defaults to "mem" if the instance is not specified.
[memory]
label= 
separator=false
interval=30

[memory]
instance=swap
interval=30

# Disk usage
#
# The directory defaults to $HOME if the instance is not specified.
# The script may be called with a optional argument to set the alert
# (defaults to 10 for 10%).
[disk]
label= 
#instance=/mnt/data
interval=30

# real ip adress and isp
[ipprovider.py]
interval=300
markup=pango

[vpn]
interval=1
#label=
separator=true
markup=pango

[ping2]
interval=15
markup=pango

#[ping]
#interval=persist
#markup=pango

[bandwidth]
instance=eno1
interval=persist
markup=pango

[termcounter]
label= 
interval=5
separator=false
separator_block_width=5

[sshkill]
full_text="|"
interval=once
command=echo ""; [[ -z "${BLOCK_BUTTON}" ]] || {{ home }}/.config/i3/blocks/killssh
separator=true

# CPU usage
[load_average]
label= 
interval=10

# Date Time
[time2]
command=echo "UTC $(date -u +%H:%M)"
interval=5

# Date Time
[time1]
command=date '+%m/%d %H:%M:%S'
interval=5

